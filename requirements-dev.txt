# Development and build-time only -- not needed to run Alfred from source.
#
#   pip install -r requirements-dev.txt
#
# PyInstaller packages the app into `dist/Alfred.exe` (see BUILD.md).
pyinstaller
# Pillow is used by the two art tools under `assets/`:
#   assets/build_icon.py           -> icon.ico
#   assets/generate_alfred_frames.py -> assets/alfred/frames/
# Both outputs are checked in, so packaging does not need Pillow --
# only regenerating the art does.
Pillow
